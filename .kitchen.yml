---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.5

suites:
  - name: default
    run_list:
      - role[qmailtoaster]
    driver:
      network:
        - [ 'forwarded_port', { guest: 80, host: 8080 } ]
